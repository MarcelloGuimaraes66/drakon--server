# Chamadas Archon - Prompts 36 a 41

Execute da raiz do repositorio:

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
    "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-36-preservar-porta-ubuntu-e-inventariar-novidades-github.md. Use somente Codex/gpt-5.5. Nao faca commit. Nao faca push."
```

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
    "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-37-integrar-identidade-central-brand-e-shared-access.md. Use somente Codex/gpt-5.5. Nao faca commit. Nao faca push."
```

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
    "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-38-resolver-worker-index-preservando-linux-camera-jobs-runtime.md. Use somente Codex/gpt-5.5. Nao faca commit. Nao faca push."
```

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
    "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-39-integrar-ui-nova-com-paridade-fluent-multiplataforma.md. Use somente Codex/gpt-5.5. Nao faca commit. Nao faca push."
```

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
    "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-40-integrar-apphost-windows-packaging-sem-vazar-winui.md. Use somente Codex/gpt-5.5. Nao faca commit. Nao faca push."
```

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
    "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-41-validacao-final-linux-webcam-rtsp-llm-packaging.md. Use somente Codex/gpt-5.5. Nao faca commit. Nao faca push."
```

