# Chamadas Archon - Prompts 29 a 35

Execute um por vez, sempre no diretorio raiz do repo:

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-29-linux-camera-start-root-cause-session-alignment.txt. Use somente Codex/gpt-5.5. Nao faca commit. Nao faca push."
```

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-30-linux-camera-session-manager-webcam-residente.txt. Use somente Codex/gpt-5.5. Nao faca commit. Nao faca push."
```

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-31-linux-backend-camera-contract-status-ui.txt. Use somente Codex/gpt-5.5. Nao faca commit. Nao faca push."
```

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-32-linux-rtsp-parity-candidates-reconnect.txt. Use somente Codex/gpt-5.5. Nao faca commit. Nao faca push."
```

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-33-linux-jobs-llm-real-inference-from-camera-artifacts.txt. Use somente Codex/gpt-5.5. Nao faca commit. Nao faca push."
```

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-34-linux-webcam-real-acceptance-ubuntu.txt. Use somente Codex/gpt-5.5. Nao faca commit. Nao faca push."
```

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-35-linux-final-packaging-camera-release-runbook.txt. Use somente Codex/gpt-5.5. Nao faca commit. Nao faca push."
```

