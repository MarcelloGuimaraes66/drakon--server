# Chamadas Archon - prompts 17 a 28

Execute um por vez, na ordem. Nao rode o proximo se o relatorio do anterior disser que ha bloqueador.

## Prompt 17

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-17-preservar-trabalho-e-preparar-sync-github.txt. Use somente Codex/gpt-5.5. Nao faca push."
```

## Prompt 18

Este prompt autoriza commits locais de checkpoint somente na branch local de integracao, sem push.

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-18-integrar-origin-main-sem-perder-porta-linux.txt. Use somente Codex/gpt-5.5. Nao faca push."
```

## Prompt 19

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-19-estabilizar-build-pos-sync-github.txt. Use somente Codex/gpt-5.5. Nao faca push."
```

## Prompt 20

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-20-linux-backend-control-plane-agent-ingress.txt. Use somente Codex/gpt-5.5. Nao faca push."
```

## Prompt 21

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-21-linux-agentcore-portable-foundation.txt. Use somente Codex/gpt-5.5. Nao faca push."
```

## Prompt 22

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-22-linux-camera-capture-framewriter-recordings.txt. Use somente Codex/gpt-5.5. Nao faca push."
```

## Prompt 23

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-23-linux-jobs-llm-inference-runtime.txt. Use somente Codex/gpt-5.5. Nao faca push."
```

## Prompt 24

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-24-linux-shared-camera-workspace-access-parity.txt. Use somente Codex/gpt-5.5. Nao faca push."
```

## Prompt 25

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-25-fluent-ui-parity-pos-github-sync.txt. Use somente Codex/gpt-5.5. Nao faca push."
```

## Prompt 26

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-26-secure-store-logging-packaging-final.txt. Use somente Codex/gpt-5.5. Nao faca push."
```

## Prompt 27

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-27-aceite-real-camera-llm-sistema-completo.txt. Use somente Codex/gpt-5.5. Nao faca push."
```

## Prompt 28

```bash
archon workflow run archon-assist --cwd "$PWD" --no-worktree \
  "Leia e execute exatamente o prompt salvo em .archon/prompts/prompt-28-release-final-paridade-100.md. Use somente Codex/gpt-5.5. Nao faca push."
```
